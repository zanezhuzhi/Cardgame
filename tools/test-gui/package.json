{
  "name": "yuhun-test-gui",
  "version": "1.0.0",
  "description": "御魂传说 - 可视化测试工具",
  "main": "main.js",
  "scripts": {
    "start": "electron .",
    "install-deps": "npm install && cd ../../shared && npm install && cd ../server && npm install"
  },
  "devDependencies": {
    "electron": "^28.0.0"
  },
  "dependencies": {}
}
